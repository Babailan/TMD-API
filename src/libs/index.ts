import watchpush from "./watchpush";
import { axiosFetcher, browserFetcher } from "./fetcher";
const libs = {
  watchpush,
  axiosFetcher,
  browserFetcher,
};

export { axiosFetcher, browserFetcher, watchpush };
export default libs;
